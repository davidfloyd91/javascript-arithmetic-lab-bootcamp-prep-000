var number;
function add(n) {
  number += n;
  console.log(number);
}
function subtract(n) {
  number -= n;
  console.log(number);
}
function multiply(n) {
  number *= n;
  console.log(number);
}
function divide(n) {
  number /= n;
  console.log(number);
}
